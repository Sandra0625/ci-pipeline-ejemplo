name: CI Pipeline

# Cuándo se ejecuta el flujo de trabajo:
on:
  push:
    branches: [ main ]        # Cada vez que haces push a la rama main
  pull_request:
    branches: [ main ]        # O cuando alguien hace un pull request hacia main

jobs:
  build:
    runs-on: ubuntu-latest    # Usa un entorno Linux para ejecutar las tareas

    steps:
      # 1️⃣ Clonar el código del repositorio
      - name: Clonar el repositorio
        uses: actions/checkout@v4

      # 2️⃣ Configurar la versión de Node.js
      - name: Configurar Node.js
        uses:
